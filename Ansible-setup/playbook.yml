---
- hosts: localhost

  vars:
    good_software:
      - chromium
      - discord
      - gnome-tweak-tool
      - telegram-desktop
      - kolourpaint
      - VirtualBox
      - git
      - vim 
    useless_software:
      - Cheese

  vars_files:
    - files/main.yml

  roles:
    - { role: packages, become: yes ,become_user: root}
    - { role: keys}
    - { role: vimsetup}
    - { role: http, become: yes ,become_user: root}




